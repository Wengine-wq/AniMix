name: Build iOS Unsigned IPA (Ad-Hoc Fix)

on: [workflow_dispatch]

jobs:
  build-ios:
    runs-on: macos-14
    
    steps:
      - name: 📥 Скачивание кода проекта
        uses: actions/checkout@v4

      - name: 🐦 Установка Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      - name: 📦 Установка зависимостей пакетов
        run: flutter pub get

      - name: 🍎 Отключение подписи во Xcode (Ad-Hoc Заглушка)
        run: |
          # Заставляем Flutter сгенерировать iOS-файлы
          flutter build ios --config-only
          
          gem install xcodeproj
          
          cat << 'EOF' > patch_xcode.rb
          require 'xcodeproj'
          project = Xcodeproj::Project.open('ios/Runner.xcodeproj')
          
          project.targets.each do |target|
            target.build_configurations.each do |config|
              config.build_settings['CODE_SIGNING_REQUIRED'] = 'NO'
              config.build_settings['CODE_SIGNING_ALLOWED'] = 'NO'
              
              # КЛЮЧЕВОЕ ИЗМЕНЕНИЕ: Используем '-', а не пустоту
              config.build_settings['CODE_SIGN_IDENTITY'] = '-'
              config.build_settings['EXPANDED_CODE_SIGN_IDENTITY'] = '-'
              config.build_settings['DEVELOPMENT_TEAM'] = ''
              config.build_settings['PROVISIONING_PROFILE_SPECIFIER'] = ''
              
              if config.build_settings['PRODUCT_BUNDLE_IDENTIFIER']&.include?('com.example')
                config.build_settings['PRODUCT_BUNDLE_IDENTIFIER'] = 'app.animix.ios'
              end
            end
          end
          
          if project.root_object.attributes['TargetAttributes']
            project.root_object.attributes['TargetAttributes'].each do |_, attributes|
              attributes['ProvisioningStyle'] = 'Manual'
            end
          end
          
          project.save
          EOF
          ruby patch_xcode.rb

      - name: 🍏 Авто-патч Podfile и установка CocoaPods
        run: |
          cd ios
          # Обновляем патч для плагинов (Pods), чтобы они тоже использовали заглушку '-'
          cat << 'EOF' > patch_podfile.rb
          text = File.read('Podfile')
          replacement = <<~RUBY
          flutter_additional_ios_build_settings(target)
              target.build_configurations.each do |config|
                config.build_settings['CODE_SIGNING_ALLOWED'] = 'NO'
                config.build_settings['CODE_SIGNING_REQUIRED'] = 'NO'
                config.build_settings['CODE_SIGN_IDENTITY'] = '-'
                config.build_settings['EXPANDED_CODE_SIGN_IDENTITY'] = '-'
                config.build_settings['DEVELOPMENT_TEAM'] = ''
              end
          RUBY
          text = text.gsub(/flutter_additional_ios_build_settings\(target\)/, replacement.strip)
          File.write('Podfile', text)
          EOF
          ruby patch_podfile.rb
          
          pod install --repo-update

      - name: 🔨 Сборка iOS приложения (No Codesign)
        run: flutter build ios --release --no-codesign

      - name: 📦 Упаковка в файл .ipa
        run: |
          cd build/ios/iphoneos/
          mkdir -p Payload
          mv Runner.app Payload/
          zip -qq -r Animix.ipa Payload

      - name: 🚀 Загрузка готового файла
        uses: actions/upload-artifact@v4
        with:
          name: Animix_iOS_App
          path: build/ios/iphoneos/Animix.ipa
          retention-days: 7
